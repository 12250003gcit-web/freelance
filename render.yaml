services:
  - type: web
    name: freelance
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: freelance-db
          property: connectionString

databases:
  - name: freelance-db
    plan: free