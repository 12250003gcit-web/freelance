services:
  - type: web
    name: freelance
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: freelance_platform
          property: connectionString

databases:
  - name: freelance_platform
    plan: free